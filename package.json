{
  "name": "modulo-extracurricular",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "start": "start.cmd",
    "build": "pnpm --filter frontend build",
    "install:all": "pnpm install",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "bcryptjs": "^3.0.3",
    "docx": "^9.7.1",
    "pizzip": "^3.2.0"
  }
}
