{
  "name": "modulo-extracurricular",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "start": "start.cmd",
    "build": "pnpm -r build",
    "install:all": "pnpm install"
  },
  "devDependencies": {
    "bcryptjs": "^3.0.3",
    "docx": "^9.7.1",
    "pizzip": "^3.2.0"
  }
}
