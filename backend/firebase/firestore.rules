rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // En desarrollo local: permitir todo. En producción: restringir según rol.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
